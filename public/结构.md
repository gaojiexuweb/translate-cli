translate-sass/
├── src/
│   ├── index.js              # 入口文件（主流程）
│   ├── config/
│   │   └── index.js          # 配置管理（OpenAI、Puppeteer、术语表等）
│   ├── core/
│   │   ├── cache.js          # LRU Cache + DiskCache
│   │   └── translator.js     # LLM 翻译核心（含术语表、重试、缓存）
│   ├── dom/
│   │   ├── extractor.js      # DOM 节点提取（Table + 普通文本）
│   │   └── injector.js       # DOM 回填（基于 data-uid）
│   └── utils/
│       ├── semanticKey.js    # 语义 Key 生成
│       ├── chunk.js          # 数组分块
│       ├── jsonParser.js     # JSON 解析工具
│       ├── sleep.js          # 延迟函数
│       ├── retry.js          # 重试机制
│       └── promisePool.js    # 并发控制
├── cache/
│   └── translate-cache.json  # 磁盘缓存（自动生成）
├── pdf/
 # 输出 PDF（自动生成）
├── package.json
└── README.md
